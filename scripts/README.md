# Install scripts

Cross-platform installers for the `contextd` binary live here.

| File | Platform |
|---|---|
| `install.sh` | macOS / Linux (`curl … \| sh`) |
| `install.ps1` | Windows (PowerShell) |

They land with the first releaseable binary (Phase 0). Until then, build or `go install` from this repo root.
