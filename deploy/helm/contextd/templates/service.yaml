apiVersion: v1
kind: Service
metadata:
  name: {{ include "contextd.fullname" . }}
  labels:
    {{- include "contextd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contextd.selectorLabels" . | nindent 4 }}
