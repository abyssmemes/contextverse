# Services

Notes about git repos / deployables belonging to this project.

ContextVerse syncs these **notes**, not the repositories themselves.

Add one markdown file per service, e.g. `api.md`, `web.md`.
