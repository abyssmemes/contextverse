---
status: active
last-validated: 2026-07-22
confidence: medium
stale-after: 30d
importance: high
---

# Example Project

Replace this folder with a real project (`projects/<your-name>/`), or delete it once you have one.

## What it is

A **scaffold** showing the documented project layout: `project.md`, `map.md`, and `services/`.

## Dependencies

```yaml
dependencies: []
# Example:
# dependencies:
#   - project: shared-libs
#     type: library
#     description: "common utilities"
#     context-needed: [README.md]
```

## Notes

- Keep this file short; put deep detail behind links in `map.md`.
- `services/` holds notes about git repos / deployables — ContextVerse does **not** sync the repos themselves.
