name: solo-default
version: 0.2.0
description: Official solo starter — full space-model layout (identity / team / projects) with one example project scaffold.
tags: [solo, starter, official, space-model]
author: ContextVerse
